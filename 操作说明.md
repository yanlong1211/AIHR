# Testin 云测 AI 招聘提效台

## 一、使用前准备

### 1. 安装 Node.js

请先安装 Node.js 18 或更高版本。安装完成后，在 PowerShell 或终端执行：

```powershell
node -v
```

能够看到版本号即可。

### 2. 解压项目文件

将整个项目文件夹解压到本地，不要只复制 `index.html`。以下文件需要放在同一目录：

- `server.js`
- `index.html`
- `.env.local`
- `.gitignore`
- `.hireflow-data.json`

## 二、配置 DeepSeek

打开项目根目录的 `.env.local`，填写 DeepSeek API Key：

```dotenv
LLM_API_KEY=你的DeepSeek_API_Key
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
LLM_PROVIDER=deepseek
USE_MOCK_AI=false
```

说明：

- `LLM_API_KEY`：必填，填写自己的真实 Key，不要加引号。
- `LLM_MODEL`：默认使用 `deepseek-chat`；如账号支持其他模型，可按需修改。DeepSeek 官方文档当前示例使用该模型。
- `USE_MOCK_AI=false`：使用真实大模型。
- 如果暂时没有 API Key，可保留为空，系统会使用规则兜底模式，其他功能仍可演示。
- 请不要把填写真实 Key 的 `.env.local` 分享给他人或上传到公开代码仓库。

## 三、启动平台

在项目目录打开 PowerShell，执行：

```powershell
node server.js
```

看到类似以下提示后，打开浏览器访问：

```text
http://localhost:4173
```

服务启动后不要关闭当前终端窗口。停止服务时按 `Ctrl + C`。

### 检查 AI 配置

访问以下地址：

```text
http://localhost:4173/api/health
```

当返回内容中的 `aiConfigured` 为 `true` 时，表示已配置真实 API Key；为 `false` 时表示当前使用规则兜底或未配置 Key。

## 四、推荐演示流程

### 1. 查看数据看板

数据看板用于查看招聘总览，包括候选人数量、招聘漏斗、待跟进事项、处理效率和岗位进度。点击左侧导航可以切换到其他功能页面。

### 2. 新建或维护岗位

进入“岗位管理”：

1. 点击“新建岗位”。
2. 填写岗位名称、所属部门、招聘负责人、编制和截止日期。
3. 点击保存。
4. 已创建的岗位可以编辑、删除或调整招聘状态。

岗位是候选人入账的关联依据。处理群消息或 PDF 简历前，请先确认目标岗位已经存在。

### 3. 处理企业微信群消息

进入“AI 消息中心”：

1. 在文本框粘贴招聘群消息，每条消息一行。
2. 选择来源群。
3. 点击“AI 提取并入库”。
4. 系统会提取候选人、岗位、招聘阶段和下一步动作。
5. 信息完整且可信度达到要求时，会自动写入候选人台账。
6. 信息缺失或可信度较低时，会进入“待人工确认”。

实际接入企业微信时，可由企业微信回调将消息发送到：

```text
POST /api/webhook/wecom
```

Demo 当前提供“同步群聊”按钮模拟消息同步。

### 4. 解析 PDF 简历

在“AI 消息中心”中：

1. 先选择 PDF 简历。
2. 选择目标岗位。
3. 点击“解析并匹配”。
4. 检查姓名、匹配分数、命中依据和待确认项。
5. 确认无误后点击“加入候选人台账”。

限制说明：

- PDF 文件不超过 8MB。
- 普通文本型 PDF 可以先提取文本，再由 DeepSeek 提取关键信息并完成岗位匹配。
- 扫描图片型 PDF 需要先 OCR，否则可能无法提取关键信息。
- 配置 API Key 后，简历关键信息提取和岗位匹配优先使用 DeepSeek；只有未配置 Key 或调用失败时才使用规则兜底，页面会显示实际处理模式。服务端使用 `https://api.deepseek.com/chat/completions`。
- AI 匹配结果仅用于辅助筛选，最终结果由 HR 确认。

### 5. 处理待人工确认

进入“AI 消息中心”的“待人工确认”区域：

- 点击“确认入账”：补充或修正姓名、岗位、招聘阶段和下一步动作后写入台账。
- 点击“驳回”：填写原因，记录会被归档，不会进入正式候选人台账。
- 如果系统提示可能存在相同候选人，可输入已有应聘者 ID 合并，避免重复建档。

候选人 ID 由系统自动生成且不可修改，用于区分同名候选人。

### 6. 管理候选人台账

进入“候选人台账”后，可以：

- 按姓名、候选人 ID、岗位或联系方式搜索。
- 按招聘负责人、岗位、招聘阶段和录用状态筛选。
- 修改候选人的岗位、招聘负责人、当前阶段和录用状态。
- 查看最后更新时间，时间精确到年月日和 24 小时制时分。
- 导出当前筛选结果或指定范围的数据。

### 7. 导出招聘数据

点击“导出招聘数据”，可选择：

- 在招候选人
- 指定岗位
- 今天更新
- 已录用人员
- 当前台账筛选结果
- 全部候选人

默认导出在招候选人，不包含已经录用或淘汰的人员。已录用人员仍保留在系统中，可在需要办理入职或人事同步时单独导出。

## 五、数据保存位置

平台数据保存在项目根目录的：

```text
.hireflow-data.json
```

刷新页面不会丢失数据。分享项目时，如果不希望带出本地演示数据，可以先备份后删除该文件，再启动服务，平台会重新生成初始数据。

## 六、常见问题

### 页面打不开

确认启动命令是在项目根目录执行，并检查终端是否出现报错。也可以访问：

```text
http://localhost:4173/api/health
```

### 4173 端口被占用

先关闭已经运行的旧服务，或在 PowerShell 中临时使用其他端口：

```powershell
$env:PORT=4174
node server.js
```

然后访问：

```text
http://localhost:4174
```

### AI 没有调用 DeepSeek

依次检查：

1. `.env.local` 是否位于 `server.js` 同一目录。
2. `LLM_API_KEY` 是否填写正确。
3. `USE_MOCK_AI` 是否为 `false`。
4. 修改配置后是否重启了 `node server.js`。
5. `/api/health` 返回的 `aiConfigured` 是否为 `true`。

### PDF 解析失败或姓名为空

当前服务端依赖系统中的 `pdftotext` 读取文本型 PDF。若环境没有该工具，或简历是扫描图片，可能无法提取文本。请先安装 Poppler，或先对扫描件进行 OCR，再重新上传。

### 数据能否多人同时使用

当前 Demo 使用本地 JSON 文件保存数据，适合单机演示，不适合多人同时写入。正式部署时应替换为数据库或腾讯在线文档智能表格，并增加企业微信回调验签、权限控制和操作日志。

## 七、安全提醒

- 不要分享包含真实 API Key 的 `.env.local`。
- 不要把真实候选人简历和联系方式提交到公开仓库。
- Demo 使用的企业信息和候选人数据仅用于演示，不代表 Testin 官方内部系统或真实数据。
- 正式使用前应补充访问权限、个人信息保护、数据留存和第三方模型合规评估。
